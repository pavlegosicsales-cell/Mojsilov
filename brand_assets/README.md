# Brand Assets

Drop logos, color/style guides, fonts, and real imagery here.

Per the frontend rules, this folder is checked **before** designing. If real assets
exist here, they are used instead of placeholders — exact logo, exact palette values,
no invented brand colors.

Currently empty — the site uses placeholders until assets are added.
